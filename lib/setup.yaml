

# !!!!!! RUN CONFIG !!!!!!!!
# wrf run setup parameter file 
run_name : TEST
     
# existing paths-- executable directories, LBCs (if they exist), geogrid file (if it exists), etc:
wrf_exe_directory : /home/rudiwill/WRF/3.8.1/WRFV3/ 
wps_exe_directory : /home/rudiwill/WRF/3.8.1/WPS 
geog_data_path : /scratch/bsu_wrf/WPS_GEOG/ 
wps_namelist_file : /home/rudiwill/WRF-INL/namelists/namelist.wps.template
input_namelist_file : /home/rudiwill/WRF-INL/namelists/namelist.input.template


#opt_geogrid_tbl_path : /home/wrudisill/LEAF/WRF/3.8.1/WPS/geogrid/
#opt_metgrid_tbl_path : /home/wrudisill/LEAF/WRF/3.8.1/WPS/geogrid/
geo_em_file : None     # If None, geogrid.exe will be run
metgrid_files : None   # If None, metgrid.exe will be run
lbc_files : None       # If None, data will be downloaded based on specified LBC condition
lbc_type : cfsr      # one of cfsr, cfsv2, ?????

        
# write locations and user parameters 
main_run_dirc: /home/rudiwill/WillWRF/TestRun2
user_email : williamrudisill@u.boisestate.edu

# run date 
run_date : 
        start_date : 1999-01-01
        end_date : 1999-01-05



# !!!! MAIN CONFIG BELOW HERE !!!!

# This stuff probably doesn't need to be changed !!!
scheduler : PBS  # SLURM or PBS
machine : INL    # name of computing environment
environment: envWRF_3.8.1_INL.sh
user: rudiwill

# computing parameters 
queue : iuc               # What are the other options ??? 
cpu_type : broadwell 
wrf_params:
           num_nodes : 2
           num_cpus : 36
           num_mpiprocs : 36
           run_time : None  # this gets dynamically updated 

wps_params:
           num_nodes : 1
           num_cpus : 36
           num_mpiprocs : 0
           ungrib_run_time : 01:00:00
           geogrid_run_time : 00:30:00
           metgrid_run_time : 00:30:00





